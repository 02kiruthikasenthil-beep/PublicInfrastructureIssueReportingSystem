# Non-Functional Requirements

- REST API with clear resource boundaries
- PostgreSQL persistence
- Passwords stored using BCrypt hashes
- JWT-based stateless authentication
- Validation on API input
- CORS restricted to the local frontend during development
- Modular backend package structure
- Frontend separated into pages, components, services and auth context
- Dockerized database for reproducible development
